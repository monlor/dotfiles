- shell:
  - 
    command: sudo apt install -y build-essential libedit-dev libbz2-dev libffi-dev libreadline-dev libssl-dev libsqlite3-dev liblzma-dev
    description: Installing development tools on Debian/Ubuntu ...
    quiet: false
    stdout: true
    stderr: true 