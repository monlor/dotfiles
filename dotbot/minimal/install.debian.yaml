- shell:
  - 
    command: ./scripts/install-apt.sh
    description: Installing apt packages ...
    quiet: false
    stdout: true
    stderr: true
  - helm plugin list diff && helm plugin install https://github.com/databus23/helm-diff || true