- defaults:
    brewfile:
      stdout: true
      stderr: true
      include: ['tap', 'brew', 'cask', 'mas']
- brewfile:
    file: package/brew/brewfile
- shell:
  - helm plugin list diff && helm plugin install https://github.com/databus23/helm-diff || true